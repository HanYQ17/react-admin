/**
 * Product的默认子路由组件
 */

import React, { Component } from 'react'

export default class ProductHome extends Component {
    render() {
        return (
            <div>
                Product的默认子路由组件
            </div>
        )
    }
}
